name: ci

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Check out the reviewed Dovecote revision
        uses: actions/checkout@v6
        with:
          repository: plethu/dovecote
          ref: 571f85f1a69ed9b77e9d6d2bbd53dc3f007688fe
          path: .ci/dovecote

      - name: Expose Dovecote at the local path used by Cargo
        run: ln -s "$GITHUB_WORKSPACE/.ci/dovecote" "$GITHUB_WORKSPACE/../carrier"

      - name: Check out the reviewed Keepsake 2 revision
        uses: actions/checkout@v6
        with:
          repository: plethu/keepsake
          ref: 890a64caffd2a2dafc2e5a1b21e45868111436a2
          path: .ci/keepsake

      - name: Expose Keepsake 2 at the local path used by Cargo
        run: ln -s "$GITHUB_WORKSPACE/.ci/keepsake" "$GITHUB_WORKSPACE/../keepsake-dovecote-2"

      - name: Install project tools
        uses: jdx/mise-action@v4

      - name: Cache cargo registry and build artifacts
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: gates-${{ hashFiles('.mise.toml', '.config/mise/conf.d/batten.toml', 'Cargo.lock') }}
          restore-keys: |
            gates-

      - name: Run project gates
        run: mise run check

      - name: Run database integration tests
        run: mise run test-db

      - name: Stop database services
        if: always()
        run: mise exec -- just db-down
