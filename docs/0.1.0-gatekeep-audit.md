# gatekeep 0.1.0 workspace audit

This is the tracked pre-release artifact for closing the spec-to-workspace gap
before publishing `gatekeep` 0.1.0.

## Closed in the current workspace

- Core decision model: typed facts, presence-only fact sets, lattice outcomes,
  source-order traces, obligations, denial shapes, and stable reason codes.
- Partial evaluation: principal-known facts reduce to residual policies, and
  completion is tested against direct evaluation.
- SQLx adapter: residual lowering, Postgres bind typing, grade projection, and
  Docker-backed differential tests wired through `make test-db`.
- Adapter seams: typed resolver/lowering/audit errors, no-op defaults, identity
  reason catalog, and test-only in-memory audit capture.
- Axum adapter: caller-owned policy/context selection with resolve→evaluate,
  observer/audit recording, localized forbidden denials, and generic hidden 404s.
- Fluent adapter: localized `ReasonCatalog` over `.ftl` bundles with typed load
  errors and deterministic stable-code fallback.
- Keepsake adapter: `FactResolver` implementation over async
  `keepsake::ActiveRelationSource`, bounded relation-id lookups,
  tenant-aware subject mapping, typed `RelationSpec` bindings, and explicit
  query-mode resolve/defer behavior.
- Examples: runnable axum authorized-list example composing localized denials
  with SQLx residual lowering, plus a keepsake-backed variant that resolves
  request facts from keepsake relations.
- Boundary validation: owned identifiers, locales, facts, and subject references
  reject empty or invalid wire values.
- send-app acceptance model: core tests cover graded case reads, participant
  facts, hidden denial shape, break-glass obligations, and authorized-list
  partial evaluation.

## Remaining 0.1.0 gaps

- No source gaps are currently tracked. Keep any future workspace additions
  behind real integration boundaries with their own tests; do not publish
  placeholder crates.
- Keepsake-side DX follow-up is tracked separately in
  [`docs/keepsake-dx-followup.md`](keepsake-dx-followup.md). It is not a
  gatekeep 0.1.0 release blocker unless the adapter example starts requiring
  local gatekeep-only in-memory fixture behavior.

## Release gate

Run these before tagging 0.1.0:

- `make check`
- `make test-db`
- `cargo publish -p gatekeep --dry-run`; publish `gatekeep` first when actually
  releasing so dependent crate dry-runs can resolve `gatekeep = "0.1.0"`.
- After `gatekeep 0.1.0` is visible in the registry:
  `cargo publish -p gatekeep-axum --dry-run`
- `cargo publish -p gatekeep-fluent --dry-run`
- `cargo publish -p gatekeep-keepsake --dry-run`
- `cargo publish -p gatekeep-sqlx --dry-run`
